using System.Collections;
using System.Collections.Generic;
using UnityEngine;

/// <summary>
/// This script is used to control an InteractButton object. These buttons are activated when the player is within range and they press the "Interact" button.
/// These buttons can only activate.
/// </summary>
public class InteractButton : MonoBehaviour {

    /// <summary>
    /// Stores the Renderer of the button so that when activated/deactivated, it can change materials to reflect beign active/deactive.
    /// </summary>
    Renderer buttonRend;

    // Use this for initialization
    void Start () {
		
	}
	
	// Update is called once per frame
	void Update () {
		
	}
}
